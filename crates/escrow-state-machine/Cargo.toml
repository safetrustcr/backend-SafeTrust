[package]
name = "escrow-state-machine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
