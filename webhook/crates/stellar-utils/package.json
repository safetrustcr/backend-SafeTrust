{
  "name": "stellar-utils",
  "version": "0.1.0",
  "description": "Rust-native Stellar utilities (Ed25519 signature verification, strkey address validation) exposed to Node.js via Neon.",
  "main": "native/index.node",
  "scripts": {
    "build": "neon build --release",
    "build:debug": "neon build"
  },
  "devDependencies": {
    "neon-cli": "^0.10.1"
  }
}
