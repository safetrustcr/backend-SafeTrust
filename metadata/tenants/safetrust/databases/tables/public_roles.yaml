table:
  name: roles
  schema: safetrust
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: roles
  custom_root_fields: {}
array_relationships:
  - name: user_roles
    using:
      foreign_key_constraint_on:
        column: role_id
        table:
          name: user_roles
          schema: safetrust
select_permissions:
  - role: admin
    permission:
      columns: "*"
      filter: {}
      allow_aggregations: true
  - role: host
    permission:
      columns:
        - id
        - name
      filter: {}
  - role: guest
    permission:
      columns:
        - id
        - name
      filter: {}
